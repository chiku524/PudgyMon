# Copy to .env and adjust. Never commit .env.
POSTGRES_USER=pudgymon
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=pudgymon_accounts
JWT_SECRET=change-me-long-random-hex
# Encrypts custodial Boing wallet secrets (defaults to JWT_SECRET if unset)
WALLET_MASTER_KEY=change-me-separate-from-jwt-ideal
# Host ports (container still uses 5432 / 8787 internally)
POSTGRES_HOST_PORT=5434
API_HOST_PORT=8788

# On-chain Market mint (treasury/deployer signs lazy NFT mint via Node + boing-sdk)
# BOING_RPC_URL=http://127.0.0.1:8545
# BOING_SECRET_HEX=0x…64 hex admin seed (same as scripts/boing/.env deployer)
# BOING_MINT_SCRIPT=../../scripts/boing/mint_skin.mjs
# BOING_SDK_PATH=C:/path/to/boing-sdk/dist/index.js
# NODE_BIN=node
# PUDGYMON_CATALOG_PATH=../../data/cosmetics/catalog.json
