name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  accounts-api:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/accounts
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: services/accounts
      - name: Check accounts API
        run: cargo check
      - name: Build accounts Docker image
        run: |
          printf '%s\n' \
            'POSTGRES_USER=pudgymon' \
            'POSTGRES_PASSWORD=ci-test-password' \
            'POSTGRES_DB=pudgymon_accounts' \
            'JWT_SECRET=ci-test-jwt-secret-not-for-prod' \
            'POSTGRES_HOST_PORT=5434' \
            'API_HOST_PORT=8788' \
            > .env
          docker compose build api
